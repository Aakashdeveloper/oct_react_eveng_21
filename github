// first time from new folder /////
git init
git add .
git commit -m m"
git remote add origin https://github.com/Aakashdeveloper/octreact.git
git push -u origin master

// next all time from same folder /////
git add .
git commit -m "m"
git push

// download all code locally 

git clone https://github.com/Aakashdeveloper/octreact

git checkout -b branchname